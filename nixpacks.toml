[phases.setup]
nixPkgs = ["python313", "gcc"]

[phases.install]
cmds = [
  "python -m venv .venv",
  ". .venv/bin/activate && pip install -r requirements.txt",
]

[start]
cmd = "python main.py --serve"
